# Production Frontend Environment Variables for Vercel
# For unified Vercel deployment (frontend + backend on same domain)

# Backend API URL - использовать пустую строку для относительных путей на Vercel
# Пустое значение означает что API будет доступен на том же домене
VITE_BACKEND_API_URL=

# These are not needed with the Python backend but kept for compatibility
VITE_ENSEMBLE_API_TOKEN=not_used_in_production
VITE_OPENAI_API_KEY=not_used_in_production
