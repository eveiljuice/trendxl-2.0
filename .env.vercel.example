# Vercel Environment Variables Example
# Copy these to your Vercel Dashboard → Settings → Environment Variables

# ========================================
# API Keys (Required)
# ========================================

# Ensemble Data API Token
# Get it from: https://dashboard.ensembledata.com/
ENSEMBLE_API_TOKEN=your-ensemble-api-token-here

# OpenAI API Key
# Get it from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Perplexity API Key
# Get it from: https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=pplx-your-perplexity-api-key-here

# JWT Secret Key (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
JWT_SECRET_KEY=your-random-secret-key-at-least-32-characters

# ========================================
# Database (Optional but recommended)
# ========================================

# Redis URL for caching (optional, but recommended)
# Free options: Upstash, Redis Cloud
# REDIS_URL=redis://default:password@your-redis-host:6379

# PostgreSQL Database (recommended for production)
# Vercel Postgres, Supabase, or PlanetScale
# DATABASE_URL=postgresql://user:password@host:5432/database

# ========================================
# Server Configuration
# ========================================

# Host (Vercel handles this automatically)
HOST=0.0.0.0

# Port (Vercel handles this automatically)
PORT=8000

# Debug mode (set to false in production)
DEBUG=false

# ========================================
# CORS Configuration
# ========================================

# Allowed origins (replace with your Vercel domain)
# Format: ["https://your-project.vercel.app","http://localhost:3000"]
CORS_ORIGINS=["https://your-project-name.vercel.app"]

# CORS regex pattern (optional, for wildcard domains)
# CORS_ORIGIN_REGEX=https?://.*\.vercel\.app$|http://localhost:\d+$

# ========================================
# Rate Limiting
# ========================================

# Max requests per minute per IP
MAX_REQUESTS_PER_MINUTE=60

# ========================================
# Caching Settings (in seconds)
# ========================================

# Profile cache TTL (30 minutes)
CACHE_PROFILE_TTL=1800

# Posts cache TTL (15 minutes)
CACHE_POSTS_TTL=900

# Trends cache TTL (5 minutes)
CACHE_TRENDS_TTL=300

# ========================================
# Frontend Build Variables
# ========================================

# Backend API URL (set during build)
# This is used by the frontend to connect to the backend
VITE_BACKEND_API_URL=https://your-project-name.vercel.app

# ========================================
# How to set these in Vercel:
# ========================================
#
# Option 1: CLI
# vercel env add ENSEMBLE_API_TOKEN
# vercel env add OPENAI_API_KEY
# etc...
#
# Option 2: Web Dashboard
# 1. Go to https://vercel.com/dashboard
# 2. Select your project
# 3. Settings → Environment Variables
# 4. Add each variable
# 5. Select environments: Production, Preview, Development
#
# ========================================

